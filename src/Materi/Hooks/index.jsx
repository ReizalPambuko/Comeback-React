import React from "react";
import CounterComponent from "./ConterComponent/CounterComponent";

const Hooks = () => {
  return (
    <div>
      <CounterComponent />
    </div>
  );
};

export default Hooks;
